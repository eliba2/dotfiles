set runtimepath^=~/.vim runtimepath+=~/.vim/after
let &packpath = &runtimepath
" source ~/.gvimrc
source ~/.vimrc

" also check ~/.config/nvim/ginit.vim,
" which is specific to Neovim's GUI clients (e.g., Neovide, Goneovim, FVim, etc.)
